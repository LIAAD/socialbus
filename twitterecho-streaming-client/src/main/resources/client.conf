## SETTINGS

## Server connection config
# this options defines the message broker where this program will send tweets to
rabbitmq.host=localhost
rabbitmq.port=5672

# defines where it will save the tweets in your local machine
#home.dir = /tmp/stream/

#if twitter stream connection fails this is the number of connection retries
connection.retries=10